services:
  - type: web
    name: api-monitoring-dashboard-backend
    runtime: python
    plan: free
    buildCommand: "pip install -r requirements.txt && pip install -e ."
    startCommand: "uvicorn backend.main:app --host 0.0.0.0 --port $PORT"